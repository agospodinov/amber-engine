#include "AmberModelLoader.h"

namespace Amber
{
    namespace IO
    {
        std::vector<Core::Entity> AmberModelLoader::loadModel(const std::string &fileName)
        {
            return std::vector<Core::Entity>();
        }
    }
}
